<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backend.cloudflare.mapper.AccountMapper">

    <resultMap id="AccountResultMap" type="com.backend.cloudflare.entity.AccountEntity">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="apiKey" column="api_key"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="cfAccountId" column="cf_account_id"/>
        <result property="tags" column="tags"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="AccountResultMap">
        SELECT id, name, api_key, description, status, cf_account_id, tags, created_at, updated_at
        FROM account ORDER BY id DESC
    </select>

    <select id="findById" resultMap="AccountResultMap">
        SELECT id, name, api_key, description, status, cf_account_id, tags, created_at, updated_at
        FROM account WHERE id = #{id}
    </select>

    <select id="findByName" resultMap="AccountResultMap">
        SELECT id, name, api_key, description, status, cf_account_id, tags, created_at, updated_at
        FROM account WHERE name = #{name}
    </select>

    <select id="findApiKeyById" resultType="string">
        SELECT api_key FROM account WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.backend.cloudflare.entity.AccountEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO account (name, api_key, description, status, cf_account_id, tags, created_at, updated_at)
        VALUES (#{name}, #{apiKey}, #{description}, #{status}, #{cfAccountId}, #{tags}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.backend.cloudflare.entity.AccountEntity">
        UPDATE account
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="apiKey != null">api_key = #{apiKey},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="cfAccountId != null">cf_account_id = #{cfAccountId},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM account WHERE id = #{id}
    </delete>

</mapper>