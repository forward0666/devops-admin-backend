<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.backend.manage.mapper.RoleMapper">

    <resultMap id="roleResultMap" type="com.backend.manage.model.Role">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="code" column="code"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="userCount" column="user_count"/>
        <result property="createdBy" column="created_by"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="findAll" resultMap="roleResultMap">
        SELECT id, name, code, description, status, user_count, created_by, updated_by, created_at, updated_at, deleted_at
        FROM roles
        WHERE deleted_at IS NULL
        ORDER BY created_at DESC
    </select>

    <select id="findById" resultMap="roleResultMap">
        SELECT id, name, code, description, status, user_count, created_by, updated_by, created_at, updated_at, deleted_at
        FROM roles
        WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="findByCode" resultMap="roleResultMap">
        SELECT id, name, code, description, status, user_count, created_by, updated_by, created_at, updated_at, deleted_at
        FROM roles
        WHERE code = #{code} AND deleted_at IS NULL
    </select>

    <insert id="insert" parameterType="com.backend.manage.model.Role" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles (name, code, description, status, user_count, created_by, updated_by, created_at, updated_at)
        VALUES (#{name}, #{code}, #{description}, #{status}, #{userCount}, #{createdBy}, #{updatedBy}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="com.backend.manage.model.Role">
        UPDATE roles
        SET name = #{name},
            code = #{code},
            description = #{description},
            status = #{status},
            updated_by = #{updatedBy},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM roles WHERE id = #{id}
    </delete>



    <select id="existsById" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM roles
        WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <select id="existsByCode" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM roles
        WHERE code = #{code} AND deleted_at IS NULL
    </select>

    <select id="existsByCodeExcludingId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM roles
        WHERE code = #{code} AND id != #{id} AND deleted_at IS NULL
    </select>

    <select id="findPermissionsByRoleId" resultType="string">
        SELECT permission_code
        FROM role_permissions
        WHERE role_id = #{roleId}
    </select>

    <insert id="insertRolePermission">
        INSERT INTO role_permissions (role_id, permission_code, created_at)
        VALUES (#{roleId}, #{permissionCode}, NOW())
    </insert>

    <delete id="deleteRolePermissions">
        DELETE FROM role_permissions
        WHERE role_id = #{roleId}
    </delete>

</mapper>
