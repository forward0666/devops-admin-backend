<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="false">

    <!-- ===================== 控制台输出 ===================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} [%thread] %-5level %logger{36} - [%X{requestId}] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================== 文件输出 ===================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/manage.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/manage.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} [%thread] %-5level %logger{36} - [%X{requestId}] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================== 根日志 ===================== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

    <!-- ===================== 指定包日志级别 ===================== -->
<!--    <logger name="com.backend.manage" level="INFO"/>-->
<!--    <logger name="org.springframework.web" level="INFO"/>-->
<!--    <logger name="org.apache.ibatis" level="INFO"/>-->
</configuration>
